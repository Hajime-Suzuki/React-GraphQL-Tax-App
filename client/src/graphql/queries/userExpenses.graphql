query getUserExpenses {
  getUserExpenses {
    id
    name
    price
    quantity
    date
    taxRate
  }
}

mutation addUserExpense($data: UserExpenseInput!) {
  addUserExpense(data: $data) {
    userExpense {
      id
      name
      price
      quantity
      date
      taxRate
    }
  }
}
